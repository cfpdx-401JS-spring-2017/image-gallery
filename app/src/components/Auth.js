import React from 'react';
import {
  Router,
  Switch,
  Link,
  withRouter
} from 'react-router';
import { connect } from 'react-redux';

const Auth = ({user, signin, signup, error, location }) => {
  return(
    <div>

    </div>
  );
};

